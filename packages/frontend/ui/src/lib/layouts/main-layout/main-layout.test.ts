import { expect, test, describe } from "bun:test";
import { MainLayoutComponent } from "./main-layout.component";

describe("MainLayoutComponent", () => {
  test("should be defined", () => {
    expect(MainLayoutComponent).toBeDefined();
  });
});
