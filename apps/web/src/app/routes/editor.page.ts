import { RouteMeta } from '@analogjs/router';
import { EditorComponent } from '../editor/editor.component';

export const routeMeta: RouteMeta = {
  title: 'Editor — Vertex IDE',
};

export default EditorComponent;
