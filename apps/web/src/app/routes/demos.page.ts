import { RouteMeta } from '@analogjs/router';
import { DemosComponent } from '../demos/demos.component';

export const routeMeta: RouteMeta = {
  title: 'Componentes — Vertex IDE',
};

export default DemosComponent;
